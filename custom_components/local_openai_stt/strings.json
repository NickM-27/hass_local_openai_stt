{
  "config": {
    "step": {
      "user": {
        "title": "Local OpenAI STT",
        "description": "Connect to your local OpenAI-compatible speech-to-text server (e.g. faster-whisper-server, whisper.cpp, LocalAI).",
        "data": {
          "base_url": "Base URL",
          "api_key": "API key"
        },
        "data_description": {
          "base_url": "Full URL ending in /v1, e.g. http://localhost:8000/v1",
          "api_key": "Optional. Many local servers do not require one."
        }
      },
      "settings": {
        "title": "Speech-to-text settings",
        "data": {
          "model": "Model",
          "prompt": "Prompt",
          "temperature": "Temperature"
        },
        "data_description": {
          "model": "Whisper model to use for transcription.",
          "prompt": "Optional bias text passed with each request. Useful for unusual vocabulary, names, or acronyms.",
          "temperature": "Sampling temperature. 0 is deterministic; higher values allow more variation."
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the server or list models. Check the base URL and API key."
    },
    "abort": {
      "already_configured": "A Local OpenAI STT entry for this base URL already exists."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Local OpenAI STT options",
        "data": {
          "model": "Model",
          "prompt": "Prompt",
          "temperature": "Temperature",
          "vad_sensitivity": "End-of-speech sensitivity",
          "vad_speech_threshold": "Speech detection threshold",
          "mic_gain": "Microphone gain",
          "debug_log": "Write per-session VAD logs",
          "debug_log_keep": "Number of session logs to keep"
        },
        "data_description": {
          "vad_sensitivity": "How quickly the utterance ends after you stop speaking. Relaxed waits longer, Aggressive cuts off sooner.",
          "vad_speech_threshold": "Silero VAD probability above which a frame is treated as speech (0–1).",
          "mic_gain": "Software amplification applied to incoming audio before VAD and transcription. Increase if quiet voices are missed; reduce if loud speech sounds distorted.",
          "debug_log": "When on, each STT call writes one log file under <config>/local_openai_stt_sessions/ with per-chunk VAD probabilities. Use for diagnosing 'never ends' problems; turn off when done.",
          "debug_log_keep": "Older session logs are deleted automatically once this many newer sessions exist."
        }
      }
    }
  },
  "selector": {
    "vad_sensitivity": {
      "options": {
        "relaxed": "Relaxed",
        "default": "Default",
        "aggressive": "Aggressive"
      }
    }
  }
}
